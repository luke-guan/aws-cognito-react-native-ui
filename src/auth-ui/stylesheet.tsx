import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    textinput: {
        borderColor: 'green',
        borderWidth: 1,
        minWidth: '50%',
        padding: 10,
        margin: 10,
    },
    hr: {
        borderColor: 'blue',
        borderWidth: 1,
        width: '50%',
    },
});
